/**
 * Mantém compatibilidade com todos os imports existentes.
 * A lógica agora vive em ActivePersonaContext.tsx.
 */
export {
  useActivePersona,
  ActivePersonaProvider,
  type Persona,
} from "./ActivePersonaContext";
